# Agent workflow

## Inputs

- Official Ralphthon plugin v0.5.0, pinned and hash-verified in the private provenance record.
- A preserved research branch whose expected base is verified as an ancestor.
- Existing synthetic graded traces and the hash-verified real-MACE infrastructure bundle `remote-trace-A`.
- Public scholarly metadata only; no private reviewer or participant records.

## Automated steps

1. Recover repository ancestry, dirty state, vendor pins, and sealed-secret metadata.
2. Run plugin, Ruff, mypy, pytest, and staged-file safety baselines.
3. Collect independent read-only topic proposals and score candidates.
4. Freeze `research_spec.json`, run deterministic Tea Time, and verify the spec content hash.
5. Generate four clean synthetic traces and 48 single-fault copies in temporary directories.
6. Compare manifest-only integrity verification with semantic trace grading exactly once per case.
7. Derive headline values from the append-only ledger and bind source SHA-256 hashes.
8. Reuse only aggregate, infrastructure-class observations from the completed L4 replay.
9. Render vector figures in R, retrieve citations programmatically, compile LaTeX without shell escape, and run claim/citation/format audits.

## Human and policy checkpoints

- The pasted mission authorized bounded promotional VESSL credit, but billable execution remained blocked because no signed sealed cost approval, approved image digest, calibrated submission schema, H2 calibration approval, or fresh remote-execution authorization existed.
- No W&B sync was performed. W&B and Weave were not installed in the project environment, and General Track 1 did not require an upload.
- Claude Fable subagents were invoked but returned a session-limit blocker. Their output was not fabricated; independent read-only Codex reviewers were used for topic selection instead.
- Tea Time at preregistration freeze produced agent-text reflection only and was excluded from evidence.

## Saved evidence

- `artifacts/research_spec/research_spec.json`
- `artifacts/auto_research/free-ralph-trace-audit/benchmark/experiment_ledger.jsonl`
- `artifacts/auto_research/free-ralph-trace-audit/benchmark/summary.json`
- `artifacts/auto_research/free-ralph-trace-audit/analysis/analysis.json`
- `remote-trace-A` (blinded locator; resolved only in `paper/internal/private_evidence_map.json`)
